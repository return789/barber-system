# 动效参考

本文档提供动效设计指南和代码参考。

## 核心原则

### 应该追求的
- ✅ 有目的的动画 - 每个动效都有理由
- ✅ 精心编排的时序 - 错开的 reveal 比随机更优雅
- ✅ 自然的缓动曲线 - 符合物理规律
- ✅ 响应用户意图 - hover、click、scroll

### 应该避免的
- ❌ 过度的 loading 动画
- ❌ 与内容竞争注意力的特效
- ❌ 不考虑性能的复杂动画
- ❌ 纯装饰性的闪烁/跳动

## CSS 动画基础

### 基本语法
```css
.element {
  animation: name duration easing delay iteration direction fill-mode;
}

/* 示例 */
.element {
  animation: fadeIn 0.6s cubic-bezier(0.16, 1, 0.3, 1) 0.2s both;
}
```

### 推荐缓动曲线
```css
:root {
  /* 标准曲线 */
  --ease-out-expo: cubic-bezier(0.16, 1, 0.3, 1);
  --ease-in-out-quart: cubic-bezier(0.76, 0, 0.24, 1);

  /* 弹性曲线 */
  --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);
  --ease-bounce: cubic-bezier(0.68, -0.55, 0.265, 1.55);

  /* 特殊曲线 */
  --ease-smooth: cubic-bezier(0.4, 0, 0.2, 1);
}
```

## 高Impact动效模式

### 1. 页面加载揭示动画
```css
/* 错开的渐入效果 */
.hero-title {
  animation: slideUp 0.8s var(--ease-out-expo) 0.1s both;
}
.hero-subtitle {
  animation: slideUp 0.8s var(--ease-out-expo) 0.2s both;
}
.hero-cta {
  animation: slideUp 0.8s var(--ease-out-expo) 0.3s both;
}
.hero-image {
  animation: scaleIn 1s var(--ease-out-expo) 0.4s both;
}

@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(40px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes scaleIn {
  from {
    opacity: 0;
    transform: scale(0.95);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}
```

### 2. Hover 微交互
```css
/* 按钮悬停 */
.btn {
  position: relative;
  overflow: hidden;
  transition: transform 0.3s var(--ease-out-expo);
}

.btn::before {
  content: '';
  position: absolute;
  inset: 0;
  background: var(--color-accent);
  transform: translateY(100%);
  transition: transform 0.4s var(--ease-out-expo);
}

.btn:hover {
  transform: translateY(-2px);
}

.btn:hover::before {
  transform: translateY(0);
}
```

### 3. 卡片悬停效果
```css
.card {
  transition: transform 0.5s var(--ease-out-expo),
              box-shadow 0.5s var(--ease-out-expo);
}

.card:hover {
  transform: translateY(-8px) scale(1.02);
  box-shadow: 0 20px 40px -12px rgba(0, 0, 0, 0.25);
}
```

### 4. 滚动触发动画
```css
.reveal {
  opacity: 0;
  transform: translateY(60px);
  transition: opacity 0.8s var(--ease-out-expo),
              transform 0.8s var(--ease-out-expo);
}

.reveal.visible {
  opacity: 1;
  transform: translateY(0);
}

/* 使用 Intersection Observer */
const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      entry.target.classList.add('visible');
    }
  });
}, { threshold: 0.1 });
```

### 5. 文字揭示动画
```css
.text-reveal {
  overflow: hidden;
}

.text-reveal span {
  display: inline-block;
  transform: translateY(100%);
  animation: revealText 0.8s var(--ease-out-expo) forwards;
}

@keyframes revealText {
  to {
    transform: translateY(0);
  }
}
```

## 复杂背景效果

### 1. 渐变动画
```css
.gradient-bg {
  background: linear-gradient(
    45deg,
    var(--color-1),
    var(--color-2),
    var(--color-3),
    var(--color-1)
  );
  background-size: 400% 400%;
  animation: gradientShift 15s ease infinite;
}

@keyframes gradientShift {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}
```

### 2. 噪点纹理叠加
```css
.noise-overlay::after {
  content: '';
  position: absolute;
  inset: 0;
  background-image: url("data:image/svg+xml,...");
  opacity: 0.03;
  pointer-events: none;
}
```

### 3. 光晕效果
```css
.glow {
  position: relative;
}

.glow::before {
  content: '';
  position: absolute;
  inset: -20px;
  background: radial-gradient(
    circle at center,
    var(--color-accent) 0%,
    transparent 70%
  );
  opacity: 0;
  transition: opacity 0.5s;
  filter: blur(20px);
}

.glow:hover::before {
  opacity: 0.5;
}
```

## 性能最佳实践

### 使用 transform 和 opacity
```css
/* ✅ 性能友好 */
transform: translateX(), translateY(), scale(), rotate()
opacity

/* ❌ 避免触发重排 */
width, height, margin, padding, top, left
```

### 使用 will-change 谨慎
```css
/* 仅在动画开始前添加 */
.will-animate {
  will-change: transform, opacity;
}

/* 动画结束后移除 */
.will-animate {
  animation: ...;
  will-change: auto;
}
```

### 减少 paint 区域
```css
/* 使用 transform 而非 box-shadow 动画 */
.shadow-element {
  will-change: transform;
}

.shadow-element:hover {
  transform: translateY(-4px);
}
```

## 动画库推荐

| 库 | 用途 | 特点 |
|---|------|------|
| [Motion](https://motion.dev/) | React 动画 | 声明式、高性能 |
| [GSAP](https://greensock.com/) | 专业动画 | 强大、时间线控制 |
| [Anime.js](https://animejs.com/) | 通用动画 | 轻量、灵活 |
| [Lottie](https://lottiefiles.com/) | 复杂动画 | AE 导出 JSON |

## 参考资源

- [Easing Functions](https://easings.net/) - 缓动函数参考
- [CSS Animation Cheatsheet](https://animate.css/) - 预设动画
- [Motion Guidelines](https://motion.ide dv.ide/) - 设计系统动画